function nameIsNotEmpty = function(input) {
  return !input.trim() ? 'Name cannot be empty' : true;
}
function streetIsNotEmpty = function(input) {
  return !input.trim() ? 'Street name cannot be empty' : true;
}
function cityIsNotEmpty = function(input) {
  return !input.trim() ? 'City cannot be empty' : true;
}
function stateIsNotEmpty = function(input) {
  return !input.trim() ? 'State cannot be empty' : true;
}
function zipIsNotEmpty = function(input) {
  return !input.trim() ? 'Zip code cannot be empty' : true;
}
function cuisineIsNotEmpty = function(input) {
  return !input.trim() ? 'Cuisine cannot be empty' : true;
}
function ratingIsNotEmpty = function(input) {
  return !input.trim() ? 'Rating cannot be empty' : true;
}
function imageIsNotEmpty = function(input) {
  return !input.trim() ? 'Image URL cannot be empty' : true;
}
function descriptionIsNotEmpty = function(input) {
  return !input.trim() ? 'Description cannot be empty' : true;
}
